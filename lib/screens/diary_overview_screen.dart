import 'package:flutter/material.dart';

class DiaryOverviewScreen extends StatelessWidget {
  const DiaryOverviewScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("All diary"),
      ),
    );
  }
}
